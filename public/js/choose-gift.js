(function () {
   // let $giftTypeIdInput1 = $('.js-gift-type-id-input1');
   // let $giftTypeIdInput2 = $('.js-gift-type-id-input2');
   // let $giftTypeIdInput3 = $('.js-gift-type-id-input3');

    // $(document).on('click', '.js-category-item', function () {
    //     console.log($(this).data('modal-id'));
    //     let modalId = $(this).data('modal-id');
    //     $('#' + modalId).modal();
    // });

   /* $(document).on('click', '.js-gift-choice1', function () {
        let $item = $(this);

        if ($item.data('item-count') === 0) {
            $('.js-modal-out-of-gifts').modal();
            return;
        }

        $giftTypeIdInput1.val($item.data('item-id'));
        console.log($giftTypeIdInput1.val());
    });

    $(document).on('click', '.js-gift-choice2', function () {
        let $item = $(this);
        if ($item.data('item-count') === 0) {
            $('.js-modal-out-of-gifts').modal();
            return;
        }
        $giftTypeIdInput2.val($item.data('item-id'));
        console.log($giftTypeIdInput2.val());
    });

    $(document).on('click', '.js-gift-choice3', function () {
        let $item = $(this);
        if ($item.data('item-count') === 0) {
            $('.js-modal-out-of-gifts').modal();
            return;
        }
        $giftTypeIdInput3.val($item.data('item-id'));
        console.log($giftTypeIdInput3.val());
    });



    $(document).on('submit', '.js-gift-choosing-form1', function (e) {
        console.log($giftTypeIdInput1.val());
        if (!$giftTypeIdInput1.val()) {
            e.preventDefault();
            $('.js-modal-gift-not-chosen').modal();
        }
    });
    $(document).on('submit', '.js-gift-choosing-form2', function (e) {
        if (!$giftTypeIdInput2.val()) {
            e.preventDefault();
            $('.js-modal-gift-not-chosen').modal();
        }
    });
    $(document).on('submit', '.js-gift-choosing-form3', function (e) {
        if (!$giftTypeIdInput3.val()) {
            e.preventDefault();
            $('.js-modal-gift-not-chosen').modal();
        }
    });
*/

}());
